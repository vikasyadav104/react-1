import { useState } from 'react'
import reactLogo from './assets/react.svg'
import viteLogo from './assets/vite.svg'
import heroImg from './assets/hero.png'
import './App.css'

function App() {
  const [count, setCount] = useState(0)

  //read some documentation of redux toolkit
  // create a store and then import configure store from redux toolkit and then create a slice and then import the slice in the store and then use the provider to wrap the app and then use the useSelector and useDispatch to get the state and dispatch the actions



  return (
    <>
  <h1> Vikas this side</h1>
    </>
  )
}

export default App
